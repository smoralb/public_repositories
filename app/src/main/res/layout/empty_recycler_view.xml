<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/iv_logo_github"
        android:layout_width="@dimen/dimen_l"
        android:layout_height="@dimen/dimen_l"
        android:layout_marginTop="@dimen/spacing_l"
        android:alpha="0.5"
        android:src="@drawable/ic_github_character"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/iv_empty_box"
        android:layout_width="@dimen/dimen_m"
        android:layout_height="@dimen/dimen_m"
        android:layout_marginTop="@dimen/spacing_s"
        android:alpha="0.5"
        android:src="@drawable/ic_empty_box_open"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/iv_logo_github" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_s"
        android:text="@string/empty_public_repositories"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/iv_empty_box" />

</android.support.constraint.ConstraintLayout>